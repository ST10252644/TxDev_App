<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="329.333dp"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@color/tertiary"
    android:backgroundTint="@color/tertiary">

    <!-- Temperature Chart Item -->
    <LinearLayout
        android:id="@+id/menu_temperature_chart"
        android:layout_width="match_parent"
        android:layout_height="57.333dp"
        android:orientation="vertical"
        android:background="@drawable/popup_menu_item_selector"
        android:backgroundTint="@color/tertiary"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:gravity="center_vertical"
        android:clickable="true"
        android:focusable="true">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:text="Temperature Chart"
            android:textColor="@color/white"
            android:textSize="14sp"
            android:fontFamily="@font/poppinsmedium"
            android:gravity="start|center_vertical"
            android:singleLine="true"
            android:ellipsize="end" />

        <!-- White line separator -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/white"
            android:layout_marginTop="2dp" />

    </LinearLayout>

    <!-- Open Door History Chart Item -->
    <LinearLayout
        android:id="@+id/menu_door_chart"
        android:layout_width="match_parent"
        android:layout_height="57.333dp"
        android:orientation="vertical"
        android:background="@drawable/popup_menu_item_selector"
        android:backgroundTint="@color/tertiary"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:gravity="center_vertical"
        android:clickable="true"
        android:focusable="true">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:text="Open Door History Chart"
            android:textColor="@color/white"
            android:textSize="14sp"
            android:fontFamily="@font/poppinsmedium"
            android:gravity="start|center_vertical"
            android:singleLine="true"
            android:ellipsize="end" />

        <!-- White line separator -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/white"
            android:layout_marginTop="2dp" />

    </LinearLayout>

    <!-- Battery Chart Item -->
    <LinearLayout
        android:id="@+id/menu_battery_chart"
        android:layout_width="match_parent"
        android:layout_height="57.333dp"
        android:orientation="vertical"
        android:background="@drawable/popup_menu_item_selector"
        android:backgroundTint="@color/tertiary"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:gravity="center_vertical"
        android:clickable="true"
        android:focusable="true">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:text="Battery Chart"
            android:textColor="@color/white"
            android:textSize="14sp"
            android:fontFamily="@font/poppinsmedium"
            android:gravity="start|center_vertical"
            android:singleLine="true"
            android:ellipsize="end" />

        <!-- White line separator (optional for last item) -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/white"
            android:layout_marginTop="2dp" />

    </LinearLayout>

    <!-- empty Item -->
    <LinearLayout
        android:id="@+id/empty_item"
        android:layout_width="match_parent"
        android:layout_height="57.333dp"
        android:orientation="vertical"
        android:background="@drawable/popup_menu_item_selector"
        android:backgroundTint="@color/tertiary"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:gravity="center_vertical"
        android:clickable="true"
        android:focusable="true">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:text="Battery Chart"
            android:visibility="gone"
            android:textColor="@color/white"
            android:textSize="14sp"
            android:fontFamily="@font/poppinsmedium"
            android:gravity="start|center_vertical"
            android:singleLine="true"
            android:ellipsize="end" />

        <!-- White line separator (optional for last item) -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/white"
            android:layout_marginTop="2dp"
            android:visibility="gone" />

    </LinearLayout>

</LinearLayout>